module Requestor
  module InternetsBaseHooks

    # def before_handle_print_it(*args)
    #   p "About to run #{self} with #{args.inspect}"
    # end
      
    # def after_handle_print_it_again(*args)
    #   p "Already ran #{self} with #{args.inspect}"
    # end

    # def on_failure_print_it_one_more_time(*args)
    # 
    #   logging = Audit::Log.new('internets_requestor_fails')
    #   logging.error("failed: #{self} #{args.inspect}")
    #   logging.error("failed: #{args[0].inspect}")
    #   logging.info("------------end")
    #   
    #   RequestorError.new(args[0], args).save
    #   
    # end
  
  end
end
